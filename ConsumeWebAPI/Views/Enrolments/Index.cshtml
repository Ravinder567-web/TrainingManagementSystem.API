@model List<ConsumeWebAPI.Models.Enrolment>



<div class="container mt-5">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h2 class="text-primary">Enrolments List</h2>
        <a asp-action="Create" class="btn btn-success">
            <i class="bi bi-plus-circle"></i>Create
        </a>
    </div>

    <div class="card shadow-sm">
        <div class="card-body p-4">
            @if (Model != null && Model.Any())
            {
                <table class="table table-striped table-bordered table-hover">
                    <thead class="table-dark">
                        <tr>
                            <th>Batch ID</th>
                            <th>User ID</th>
                            <th>Status</th>
                            <th>Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>@item.BatchId</td>
                                <td>@item.UserId</td>
                                <td>@item.Status</td>
                                <td>
                                    <div class="btn-group" role="group">
                                        <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-primary px-4 py-2">Edit</a>
                                        <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-danger px-4 py-2">Delete</a>
                                    </div>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            }
            else
            {
                <div class="alert alert-info text-center">
                    No enrolments found.
                </div>
            }
        </div>
    </div>
</div>
